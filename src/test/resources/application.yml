spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
  jpa:
    database: mysql
    open-in-view: false
    show-sql: false
    hibernate:
      ddl-auto: create-drop
      use-new-id-generator-mappings: false
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate.format_sql: true
      hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
      hibernate.default_batch_fetch_size: 30
  datasource:
    url: jdbc:mysql://kurlyflow-db.cmhxc9hlcnku.ap-northeast-2.rds.amazonaws.com/kurlyflow?autoReconnect=true # 변경해주세요
    username: admin
    password: kurlyflow
    driver-class-name: com.mysql.cj.jdbc.Driver # mysql 8버전
cloud:
  aws:
    s3:
      bucket:
        url: https://test-spring.s3.ap-northeast-2.amazonaws.com/
        name: p2p
    credentials:
      instance-profile: true
      accessKey: accessKey
      secretKey: secretKey
    region:
      static: ap-northeast-2
    stack:
      auto: false

jwt:
  secretKey: teststestssdfstsset